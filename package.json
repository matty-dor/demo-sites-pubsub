{
  "name": "demo-sites",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^4.11.0"
  }
}
